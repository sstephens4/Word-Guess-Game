// Create an array of Disney Characters
var disneyCharacters = ['Daffy Duck', 'Minnie Mouse', 'Daisy Duck', 'Goofy',
    'Tinkerbell', 'Peter Pan', 'Pluto', 'Mickey Mouse', 'Ariel', 'Belle'];

// Choose character randomly
var randNum = Math.floor[Math.random() * disneyCharacters.length]);
// Create underscores based on length of chosen character.
var chosenWord = disneyCharacters[randNum];
console.log("Hello World");
// Get users guess
// Check if guess is right
// if right push to right array
// if wrong push to wrong array